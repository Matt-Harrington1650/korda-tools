export const migrateTools = (raw: unknown): unknown => {
  // TODO(migrations): add tools schema migrations when version changes.
  return raw;
};

export const migrateSettings = (raw: unknown): unknown => {
  // TODO(migrations): add settings schema migrations when version changes.
  return raw;
};

export const migrateToolRunLogs = (raw: unknown): unknown => {
  // TODO(migrations): add tool run log schema migrations when version changes.
  return raw;
};
